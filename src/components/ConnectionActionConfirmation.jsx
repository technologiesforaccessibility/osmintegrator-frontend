import Button from '@mui/material/Button';

const ConnectionActionConfirmation = () => {
  return (
    <div>
      Are you sure you want to delete/approve this connection?
      <Button onClick={() => {}}>YES</Button>
      <Button onClick={() => {}}>NO</Button>
    </div>
  );
};

export default ConnectionActionConfirmation;
